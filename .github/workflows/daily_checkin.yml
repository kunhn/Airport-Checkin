name: CI Workflow

on:
  push:
    branches:
      - main

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build project
        run: python main.py

      - name: Run tests
        run: |
          # 在这里添加运行测试的命令或脚本
